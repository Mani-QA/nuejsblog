{
  "name": "nuejs",
  "version": "1.0.0",
  "description": "Nue JS Blog",
  "private": true,
  "scripts": {
    "build": "nue build --root ./_src --output ./dist/dev --prod"
  },
  "devDependencies": {
    "nuekit": "^1.0.0-RC.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
