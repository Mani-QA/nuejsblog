{
  "name": "nuejs",
  "version": "1.0.0",
  "description": "Nue JS Blog",
  "private": true,
  "scripts": {
    "dev": "nue hot",
    "build": "nue build --dist public", // Example: Builds to a 'public' directory
    "start": "nue serve"
  },
  "devDependencies": {
    "nuekit": "^1.0.0-RC.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
